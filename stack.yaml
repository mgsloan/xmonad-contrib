# Build xmonad-contrib against the river backend, to measure how much of it
# survives.  The xmonad checkout next door is built with -f river, so it
# provides no Graphics.X11 and a deliberately reduced API.
resolver: lts-24.53

packages:
- ./
- ../xmonad-river

flags:
  xmonad:
    river: true
